'use client'
import { useEffect, useState } from 'react'
import { supabase } from '../../lib/supabase/client'
import { requireAdminSession } from '../../lib/admin/auth'
import styles from './admin.module.css'
type Order={id:string;order_reference:string;customer_name:string;mobile_number:string;delivery_method:string;payment_method:string;upfront_amount:number|string;rider_collectible_amount:number|string;showroom_payable_amount:number|string;payment_status:string;order_status:string;payment_proof_path:string|null;created_at:string;order_notes:string|null}
export function OrdersTable(){const [orders,setOrders]=useState<Order[]>([]);const [error,setError]=useState<string|null>(null);async function load(){const {data,error}=await supabase.from('orders').select('id,order_reference,customer_name,mobile_number,delivery_method,payment_method,upfront_amount,rider_collectible_amount,showroom_payable_amount,payment_status,order_status,payment_proof_path,created_at,order_notes').order('created_at',{ascending:false});if(error)setError(error.message);else setOrders((data??[]) as Order[])}useEffect(()=>{load()},[]);async function proof(path:string){const {data,error}=await supabase.storage.from('payment-proofs').createSignedUrl(path,60);if(error)return setError(error.message);window.open(data.signedUrl,'_blank','noopener,noreferrer')}async function update(order:Order,field:'payment_status'|'order_status',value:string){try{await requireAdminSession();const {error}=await supabase.from('orders').update({[field]:value,updated_at:new Date().toISOString()}).eq('id',order.id);if(error)throw error;load()}catch(e){setError(e instanceof Error?e.message:'Order update failed.')}}return <section className={styles.panel}><div className={styles.panelHeader}><h2>Orders</h2><span>{orders.length} orders</span></div>{error&&<p className={styles.errorMessage}>{error}</p>}<div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Reference</th><th>Customer</th><th>Delivery</th><th>Payment</th><th>Amounts</th><th>Status</th><th>Proof</th></tr></thead><tbody>{orders.map(order=><tr key={order.id}><td>{order.order_reference}</td><td>{order.customer_name}<br/>{order.mobile_number}</td><td>{order.delivery_method.replaceAll('_',' ')}</td><td>{order.payment_method.replaceAll('_',' ')}</td><td>Upfront ₱{order.upfront_amount}<br/>Rider/showroom ₱{Number(order.rider_collectible_amount)||Number(order.showroom_payable_amount)}</td><td><select value={order.payment_status} onChange={e=>update(order,'payment_status',e.target.value)}><option value="pending_verification">Pending verification</option><option value="verified">Verified</option><option value="rejected">Rejected</option></select><select value={order.order_status} onChange={e=>update(order,'order_status',e.target.value)}><option value="pending">Pending</option><option value="reservation_pending">Reservation pending</option><option value="confirmed">Confirmed</option><option value="cancelled">Cancelled</option></select></td><td>{order.payment_proof_path?<button className={styles.tableAction} onClick={()=>proof(order.payment_proof_path!)}>View proof</button>:'—'}</td></tr>)}</tbody></table></div></section>}
