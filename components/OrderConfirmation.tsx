'use client'
import Link from 'next/link'
import { useEffect, useState } from 'react'
export function OrderConfirmation(){const [order,setOrder]=useState<Record<string,unknown>|null>(null);useEffect(()=>{try{setOrder(JSON.parse(sessionStorage.getItem('hydro-order-confirmation')??'null'))}catch{}},[]);if(!order)return <section className="section"><h1>Order confirmation unavailable</h1><p>Please contact Hydro Blasters MNL if you need help with an order.</p></section>;return <section className="section"><p className="eyebrow">Order received</p><h1>Thank you, {String(order.customer_name)}</h1><p>Order reference: <strong>{String(order.order_reference)}</strong></p><p>Delivery: {String(order.delivery_method).replaceAll('_',' ')}</p><p>Payment: {String(order.payment_method).replaceAll('_',' ')}</p><p>Payment status: Pending Verification</p><p>Amount Due Now: ₱{String(order.upfront_amount)}</p><p>Payment verification may take up to 24 hours. If your payment has not yet been verified after 24 hours, you may follow up through the Facebook account: Hydro Blasters MNL.</p><Link className="primary-button" href="/shop">Continue shopping</Link></section>}
