'use client'
import { useEffect, useState } from 'react'
import { useRouter } from 'next/navigation'
import { supabase } from '../lib/supabase/client'
import { useCart } from './CartProvider'
import { PaymentQr } from './PaymentQr'
const peso=(n:number)=>new Intl.NumberFormat('en-PH',{style:'currency',currency:'PHP'}).format(n)
const initial={customer_name:'',mobile_number:'',house_unit:'',street:'',barangay:'',city_municipality:'',region:'',postal_code:'',order_notes:''}
async function base64(file:File){return new Promise<string>((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(String(r.result).split(',')[1]);r.onerror=reject;r.readAsDataURL(file)})}
export function GuestCheckout(){const {lines,subtotal,clear}=useCart();const router=useRouter();const [form,setForm]=useState(initial);const [delivery,setDelivery]=useState<'nationwide_delivery'|'showroom_pickup'>('nationwide_delivery');const [payment,setPayment]=useState('gcash');const [proof,setProof]=useState<File|null>(null);const [reservation,setReservation]=useState(false);const [codConfirm,setCodConfirm]=useState(false);const [error,setError]=useState<string|null>(null);const [saving,setSaving]=useState(false);const [qrAvailable,setQrAvailable]=useState(true);const bulky=lines.some(x=>x.shipping_classification==='bulky');const shipping=delivery==='nationwide_delivery'?(bulky?199:149):0;const cod=payment==='cash_on_delivery'?Math.ceil(subtotal*.01):0;const pickup=payment==='pay_upon_pickup';const upfront=pickup?0:payment==='cash_on_delivery'?shipping+cod:subtotal+shipping;const proofNeeded=!pickup;useEffect(()=>{if(delivery==='nationwide_delivery'&&!['gcash','bank_transfer','cash_on_delivery'].includes(payment))setPayment('gcash');if(delivery==='showroom_pickup'&&!['gcash','bank_transfer','pay_upon_pickup'].includes(payment))setPayment('gcash')},[delivery,payment]);if(!lines.length)return <section className="section"><h1>Your cart is empty</h1></section>;const update=(k:keyof typeof initial,v:string)=>setForm(c=>({...c,[k]:v}));async function submit(e:React.FormEvent){e.preventDefault();setError(null);if(proofNeeded&&!qrAvailable)return setError('Payment details are temporarily unavailable. Please contact Hydro Blasters MNL before sending payment.');if(proofNeeded&&!proof)return setError('A payment screenshot is required.');if(pickup&&!reservation)return setError('Confirm that this is only a reservation request.');if(payment==='cash_on_delivery'&&!codConfirm)return setError('Confirm the COD payment requirement.');if(proof&&(!['image/jpeg','image/png','image/webp'].includes(proof.type)||proof.size>5*1024*1024))return setError('Payment proof must be JPG, PNG, or WebP and 5 MB or smaller.');setSaving(true);try{const {data,error:invokeError}=await supabase.functions.invoke('create-guest-order',{body:{...form,delivery_method:delivery,payment_method:payment,items:lines.map(x=>({product_id:x.id,quantity:x.quantity})),idempotency_key:crypto.randomUUID(),payment_proof:proof?{base64:await base64(proof),contentType:proof.type}:null}});if(invokeError)throw invokeError;if(data?.error)throw new Error(data.error);sessionStorage.setItem('hydro-order-confirmation',JSON.stringify({...data.order,customer_name:form.customer_name,delivery_method:delivery,payment_method:payment}));clear();router.push('/order-confirmation')}catch(c){setError(c instanceof Error?c.message:'Order could not be placed.')}finally{setSaving(false)}}return <section className="section checkout-page"><header className="checkout-heading"><p className="eyebrow">Guest checkout</p><h1>Checkout</h1><p>Complete your details to submit your order for review.</p></header><form onSubmit={submit} className="checkout-layout">{error&&<p className="checkout-error" role="alert">{error}</p>}<div className="checkout-fields"><section className="checkout-card"><h2>Customer Information</h2><div className="checkout-grid"><label>Full Name <em>Required</em><input required value={form.customer_name} onChange={e=>update('customer_name',e.target.value)}/></label><label>Mobile Number <em>Required</em><input required value={form.mobile_number} onChange={e=>update('mobile_number',e.target.value)}/></label></div></section><section className="checkout-card"><h2>Delivery</h2><label>Delivery Method <em>Required</em><select value={delivery} onChange={e=>setDelivery(e.target.value as typeof delivery)}><option value="nationwide_delivery">Nationwide Delivery</option><option value="showroom_pickup">Showroom Pickup</option></select></label>{delivery==='nationwide_delivery'&&<div className="checkout-grid">{(['house_unit','street','barangay','city_municipality','region','postal_code'] as const).map(k=><label key={k}>{k.replaceAll('_',' ')} <em>Required</em><input required value={form[k]} onChange={e=>update(k,e.target.value)}/></label>)}</div>}<label>Order Notes <em>Optional</em><textarea value={form.order_notes} onChange={e=>update('order_notes',e.target.value)}/></label></section><section className="checkout-card"><h2>Payment</h2><label>Payment Method <em>Required</em><select value={payment} onChange={e=>setPayment(e.target.value)}>{delivery==='nationwide_delivery'?<><option value="gcash">GCash</option><option value="bank_transfer">Bank transfer</option><option value="cash_on_delivery">Cash on Delivery</option></>:<><option value="gcash">GCash</option><option value="bank_transfer">Bank transfer</option><option value="pay_upon_pickup">Pay upon pickup</option></>}</select></label>{proofNeeded&&<PaymentQr method={payment} amount={upfront} onAvailabilityChange={setQrAvailable}/>} {proofNeeded?<div className="proof-card"><strong>Payment Screenshot Upload</strong><p>After payment, upload a screenshot of the successful transaction below.</p><input required type="file" accept="image/jpeg,image/png,image/webp" onChange={e=>setProof(e.target.files?.[0]??null)}/><p>Accepted: JPG, PNG, WebP · Maximum file size: 5 MB</p>{proof&&<img src={URL.createObjectURL(proof)} alt="Payment screenshot preview"/>}</div>:<label className="checkout-check"><input type="checkbox" checked={reservation} onChange={e=>setReservation(e.target.checked)}/> I understand that this is only a reservation request and I must wait for Hydro Blasters MNL to confirm before visiting.</label>}{payment==='cash_on_delivery'&&<label className="checkout-check"><input type="checkbox" checked={codConfirm} onChange={e=>setCodConfirm(e.target.checked)}/> I understand that my COD order will only be processed after the shipping fee and COD service fee are paid and verified.</label>}</section></div><aside className="checkout-summary"><h2>Order Summary</h2>{lines.map(x=><p key={x.id}>{x.name} × {x.quantity} — {peso(Number(x.price)*x.quantity)}</p>)}<dl><div><dt>Subtotal</dt><dd>{peso(subtotal)}</dd></div><div><dt>Shipping</dt><dd>{peso(shipping)}</dd></div>{cod>0&&<div><dt>COD Fee</dt><dd>{peso(cod)}</dd></div>}<div className="checkout-total"><dt>Total</dt><dd>{peso(subtotal+shipping+cod)}</dd></div></dl><button className="secondary-button" disabled={saving||proofNeeded&&!qrAvailable} type="submit">{saving?'Placing order…':'Place Order'}</button></aside></form></section>}
