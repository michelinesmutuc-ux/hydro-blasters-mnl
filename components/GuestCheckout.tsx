'use client'

import { useMemo, useState } from 'react'
import { useRouter } from 'next/navigation'
import { supabase } from '../lib/supabase/client'
import { useCart } from './CartProvider'

const peso = (value: number) => new Intl.NumberFormat('en-PH', { style: 'currency', currency: 'PHP' }).format(value)
const initial = { customer_name: '', mobile_number: '', house_unit: '', street: '', barangay: '', city_municipality: '', region: '', postal_code: '', order_notes: '' }
async function base64(file: File) { return new Promise<string>((resolve, reject) => { const reader = new FileReader(); reader.onload = () => resolve(String(reader.result).split(',')[1]); reader.onerror = reject; reader.readAsDataURL(file) }) }

export function GuestCheckout() {
  const { lines, subtotal, clear } = useCart(); const router = useRouter()
  const [form, setForm] = useState(initial); const [delivery, setDelivery] = useState<'nationwide_delivery' | 'showroom_pickup'>('nationwide_delivery'); const [payment, setPayment] = useState('gcash'); const [proof, setProof] = useState<File | null>(null); const [reservation, setReservation] = useState(false); const [codConfirm, setCodConfirm] = useState(false); const [error, setError] = useState<string | null>(null); const [saving, setSaving] = useState(false)
  const bulky = lines.some((line) => line.shipping_classification === 'bulky'); const shipping = delivery === 'nationwide_delivery' ? (bulky ? 199 : 149) : 0; const cod = payment === 'cash_on_delivery' ? Math.ceil(subtotal * .01) : 0; const pickup = payment === 'pay_upon_pickup'; const upfront = pickup ? 0 : payment === 'cash_on_delivery' ? shipping + cod : subtotal + shipping; const rider = payment === 'cash_on_delivery' ? subtotal : 0; const showroom = pickup ? subtotal : 0; const proofNeeded = !pickup
  useMemo(() => { if (delivery === 'nationwide_delivery' && !['gcash', 'bank_transfer', 'cash_on_delivery'].includes(payment)) setPayment('gcash'); if (delivery === 'showroom_pickup' && !['gcash', 'bank_transfer', 'pay_upon_pickup'].includes(payment)) setPayment('gcash') }, [delivery, payment])
  if (!lines.length) return <section className="section"><h1>Your cart is empty</h1></section>
  const update = (key: keyof typeof initial, value: string) => setForm((current) => ({ ...current, [key]: value }))
  async function submit(event: React.FormEvent) { event.preventDefault(); setError(null); if (proofNeeded && !proof) return setError('A payment screenshot is required.'); if (pickup && !reservation) return setError('Confirm that this is only a reservation request.'); if (payment === 'cash_on_delivery' && !codConfirm) return setError('Confirm the COD payment requirement.'); if (proof && (!['image/jpeg', 'image/png', 'image/webp'].includes(proof.type) || proof.size > 5 * 1024 * 1024)) return setError('Payment proof must be JPG, PNG, or WebP and 5 MB or smaller.'); setSaving(true); try { const proofPayload = proof ? { base64: await base64(proof), contentType: proof.type } : null; const { data, error: invokeError } = await supabase.functions.invoke('create-guest-order', { body: { ...form, delivery_method: delivery, payment_method: payment, items: lines.map((line) => ({ product_id: line.id, quantity: line.quantity })), idempotency_key: crypto.randomUUID(), payment_proof: proofPayload } }); if (invokeError) throw invokeError; if (data?.error) throw new Error(data.error); sessionStorage.setItem('hydro-order-confirmation', JSON.stringify({ ...data.order, customer_name: form.customer_name, delivery_method: delivery, payment_method: payment })); clear(); router.push('/order-confirmation') } catch (cause) { setError(cause instanceof Error ? cause.message : 'Order could not be placed.') } finally { setSaving(false) } }
  return <section className="section checkout-page"><header className="checkout-heading"><p className="eyebrow">Guest checkout</p><h1>Checkout</h1><p>Complete your details to submit your order for review.</p></header><form onSubmit={submit} className="checkout-layout">{error && <p className="checkout-error" role="alert">{error}</p>}<div className="checkout-fields"><section className="checkout-card"><h2>Customer Information</h2><div className="checkout-grid"><label>Full Name <em>Required</em><input required value={form.customer_name} onChange={(event) => update('customer_name', event.target.value)} /></label><label>Mobile Number <em>Required</em><input required inputMode="tel" value={form.mobile_number} onChange={(event) => update('mobile_number', event.target.value)} /></label></div></section><section className="checkout-card"><h2>Delivery</h2><label>Delivery Method <em>Required</em><select value={delivery} onChange={(event) => setDelivery(event.target.value as typeof delivery)}><option value="nationwide_delivery">Nationwide Delivery</option><option value="showroom_pickup">Showroom Pickup</option></select></label>{delivery === 'nationwide_delivery' && <><div className="checkout-helper"><strong>Complete Address</strong><p>Please provide your complete delivery address including:</p><ul><li>House or Unit Number</li><li>Street</li><li>Barangay</li><li>City or Municipality</li><li>Region</li><li>Postal Code</li></ul></div><div className="checkout-grid"><label>House / Unit <em>Required</em><input required value={form.house_unit} onChange={(event) => update('house_unit', event.target.value)} /></label><label>Street <em>Required</em><input required value={form.street} onChange={(event) => update('street', event.target.value)} /></label><label>Barangay <em>Required</em><input required value={form.barangay} onChange={(event) => update('barangay', event.target.value)} /></label><label>City / Municipality <em>Required</em><input required value={form.city_municipality} onChange={(event) => update('city_municipality', event.target.value)} /></label><label>Region <em>Required</em><input required value={form.region} onChange={(event) => update('region', event.target.value)} /></label><label>Postal Code <em>Required</em><input required value={form.postal_code} onChange={(event) => update('postal_code', event.target.value)} /></label></div></>}<label>Order Notes <em>Optional</em><textarea value={form.order_notes} onChange={(event) => update('order_notes', event.target.value)} /></label></section><section className="checkout-card"><h2>Payment</h2><label>Payment Method <em>Required</em><select value={payment} onChange={(event) => setPayment(event.target.value)}>{delivery === 'nationwide_delivery' ? <><option value="gcash">GCash</option><option value="bank_transfer">Bank transfer</option><option value="cash_on_delivery">Cash on Delivery</option></> : <><option value="gcash">GCash</option><option value="bank_transfer">Bank transfer</option><option value="pay_upon_pickup">Pay upon pickup</option></>}</select></label><div className="checkout-helper"><strong>Payment Instructions</strong><p>{pickup ? 'Your reservation is only confirmed after Hydro Blasters MNL checks availability.' : payment === 'cash_on_delivery' ? 'Pay the nationwide shipping fee and COD service fee before your order can be processed.' : 'Payment verification may take up to 24 hours.'}</p></div>{proofNeeded ? <div className="proof-card"><strong>Payment Screenshot Upload</strong><p>A screenshot is required before this order can be placed.</p><input required type="file" accept="image/jpeg,image/png,image/webp" onChange={(event) => setProof(event.target.files?.[0] ?? null)} /><p>Accepted: JPG, PNG, WebP · Maximum file size: 5 MB</p>{proof && <img src={URL.createObjectURL(proof)} alt="Payment screenshot preview" />}</div> : <label className="checkout-check"><input type="checkbox" checked={reservation} onChange={(event) => setReservation(event.target.checked)} /> I understand that this is only a reservation request and I must wait for Hydro Blasters MNL to confirm before visiting.</label>}{payment === 'cash_on_delivery' && <label className="checkout-check"><input type="checkbox" checked={codConfirm} onChange={(event) => setCodConfirm(event.target.checked)} /> I understand that my COD order will only be processed after the shipping fee and COD service fee are paid and verified.</label>}</section></div><aside className="checkout-summary"><h2>Order Summary</h2><div className="checkout-products">{lines.map((line) => <p key={line.id}><span>{line.name} × {line.quantity}</span><strong>{peso(Number(line.price) * line.quantity)}</strong></p>)}</div><dl><div><dt>Subtotal</dt><dd>{peso(subtotal)}</dd></div><div><dt>Shipping</dt><dd>{peso(shipping)}</dd></div>{cod > 0 && <div><dt>COD Fee</dt><dd>{peso(cod)}</dd></div>}<div className="checkout-total"><dt>Total</dt><dd>{peso(subtotal + shipping + cod)}</dd></div></dl><button className="secondary-button" disabled={saving} type="submit">{saving ? 'Placing order…' : 'Place Order'}</button><p>Payment status: Pending Verification</p></aside></form></section>
}
