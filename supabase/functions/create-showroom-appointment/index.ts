import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'
const headers={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type','Access-Control-Allow-Methods':'POST, OPTIONS','Content-Type':'application/json'}
const reply=(body:Record<string,string>,status=200)=>new Response(JSON.stringify(body),{status,headers})
Deno.serve(async request=>{if(request.method==='OPTIONS')return new Response('ok',{headers});if(request.method!=='POST')return reply({error:'Method not allowed.'},405);try{const body=await request.json();for(const key of ['customer_name','mobile_number','preferred_date','preferred_time','products_of_interest'])if(!String(body[key]??'').trim())return reply({error:'Please complete all required appointment fields.'},400);if(body.agreement!==true)return reply({error:'Please confirm the appointment-only showroom agreement.'},400);const admin=createClient(Deno.env.get('SUPABASE_URL')!,Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!);const {error}=await admin.from('showroom_appointments').insert({customer_name:body.customer_name.trim(),mobile_number:body.mobile_number.trim(),preferred_date:body.preferred_date,preferred_time:body.preferred_time,products_of_interest:body.products_of_interest.trim(),additional_notes:body.additional_notes?.trim()||null,status:'pending'});if(error)throw error;return reply({message:'Appointment request received.'},201)}catch{return reply({error:'Your appointment request could not be sent. Please try again.'},500)}})
